/**
 * Supabase connection (anon URL + key are safe to commit when schema.sql RLS is applied).
 * Set edit_key only in Supabase SQL — never in this file. Editors enter it in the app UI.
 */
window.CONTENT_FLOW_SUPABASE = {
  url: "",
  anonKey: "",
  stateId: "default"
};
